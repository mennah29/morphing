# ... تكملة من الخطوة السابقة (Install Buildozer) ...
      - name: Install Buildozer and Dependencies
        run: |
          # ... (باقي أوامر التثبيت) ...
          pip3 install buildozer cython

      # 🚨 الخطوة الجديدة: قبول تراخيص Android SDK 🚨
      - name: Accept SDK Licenses
        run: |
          # هذا الأمر يجبر Buildozer على الموافقة على جميع التراخيص المطلوبة
          buildozer android update

      # 📦 الخطوة التالية: حل مشكلة الملف المفقود وبدء التجميع
      - name: Run Buildozer
        run: |
          buildozer init
          buildozer android debug

      # ... (باقي الكود) ...
