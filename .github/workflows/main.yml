name: Buildozer Android Build

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 🛠️ الخطوة 1: تثبيت Buildozer والمتطلبات يدوياً
      - name: Install Buildozer and Dependencies
        run: |
          echo "Starting Manual Setup..."
          
          # تثبيت متطلبات النظام الأساسية
          sudo apt update
          sudo apt install -y git build-essential zlib1g-dev libncurses5-dev libffi-dev libssl-dev python3 python3-pip python3-setuptools
          
          # تثبيت Buildozer و Cython (لحزمة Kivy/MediaPipe)
          pip3 install --upgrade pip
          pip3 install buildozer cython  # 🚨 Cython تمت إضافته هنا

      # 📦 الخطوة 2: حل مشكلة الملف المفقود وبدء التجميع
      - name: Run Buildozer
        run: |
          # 🚨 حل خطأ "No buildozer.spec found"
          buildozer init
          # بدء البناء
          buildozer android debug

      # ⬆️ الخطوة 3: رفع الـ APK
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-debug-apk
          path: bin/*.apk
